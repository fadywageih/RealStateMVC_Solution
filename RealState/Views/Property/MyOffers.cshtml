@model IEnumerable<PropertyDto>
@{
    ViewData["Title"] = "My Offers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mx-auto px-4 py-8">
    <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-bold text-gray-800">My Offers</h2>
        <a asp-action="Create" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">Add New Property</a>
    </div>

    @if (Model.Any())
    {
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            @foreach (var property in Model)
            {
                <div class="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-xl transition-shadow duration-300">
                    <div class="h-56 bg-gray-200 overflow-hidden">
                        @if (property.ImageUrls.Any())
                        {
                            <img src="@property.ImageUrls.First()"
                                 alt="@property.Title"
                                 class="w-full h-full object-cover" />
                        }
                        else
                        {
                            <div class="w-full h-full flex items-center justify-center bg-gray-100 text-gray-400">
                                No Image
                            </div>
                        }
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start mb-2">
                            <h3 class="text-lg font-bold text-gray-900 line-clamp-1">@property.Title</h3>
                            @if (property.Price.HasValue)
                            {
                                <span class="text-lg font-semibold text-green-600">
                                    @property.Price.Value.ToString("C0")
                                </span>
                            }
                        </div>

                        <p class="text-sm text-gray-600 mb-3 line-clamp-2">
                            @(string.IsNullOrEmpty(property.Description) ? "No description." : property.Description)
                        </p>

                        <div class="space-y-2 text-sm text-gray-700">
                            <div class="flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                                </svg>
                                <span>@property.City</span>
                            </div>

                            <div class="flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z" />
                                </svg>
                                <span class="capitalize">@property.PropertyType</span>
                            </div>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-between items-center">
                            <span class="text-xs text-gray-500">
                                Posted: @property.CreatedOn.ToString("MMM dd, yyyy")
                            </span>
                            <div class="space-x-2">
                                <a asp-action="Details" asp-route-id="@property.Id" class="text-blue-600 hover:underline text-sm">View</a>
                                <a asp-action="Edit" asp-route-id="@property.Id" class="text-green-600 hover:underline text-sm">Edit</a>
                                <a asp-action="Delete" asp-route-id="@property.Id" class="text-red-600 hover:underline text-sm"
                                   onclick="return confirm('Are you sure you want to delete this property?');">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="text-center py-12">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-gray-400 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
            <h3 class="text-lg font-medium text-gray-900">You haven't added any properties yet</h3>
            <p class="text-gray-600 mt-2">Start by creating your first property listing.</p>
            <a asp-action="Create" class="mt-4 inline-block px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">Add Property</a>
        </div>
    }
</div>